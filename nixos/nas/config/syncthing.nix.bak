{

  services = {
      syncthing = {
          enable = true;
          user = "simon";
          dataDir = "/home/simon/Sync";                  # Default folder for new synced folders
          configDir = "/home/simon/.config/syncthing";   # Folder for Syncthing's settings and keys
          guiAddress = "0.0.0.0:8384";
      };
  };

  networking.firewall.allowedTCPPorts = [ 8384 22000 ];
  networking.firewall.allowedUDPPorts = [ 22000 21027 ];

  services.syncthing.settings.gui = {
      user = "simon";
      password = "dont hack me";
  };

}
